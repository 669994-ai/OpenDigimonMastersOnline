# Tests

Workspace verification currently lives mostly alongside each crate as unit, property, and integration tests.

This top-level `tests/` directory is reserved for broader cross-service scenarios such as:

- multi-service integration coverage
- protocol compatibility fixtures
- smoke and load-oriented validation
